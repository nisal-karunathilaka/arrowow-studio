"""Arrowow Studio — UI layer (Streamlit driver + background jobs).

This package is a thin presentation layer over the real ADK pipeline in app/adk.
It never reimplements creative/media logic — it runs the genuine agents/tools in
human-in-the-loop segments and renders their state for review.
"""
